[tool.poetry]
name = "ortho-token-optimizer"
version = "0.1.0"
description = "Context ranking and token optimization"
authors = ["urbra <urbrain369@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
