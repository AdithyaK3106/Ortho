[tool.poetry]
name = "ortho-context-hub"
version = "0.1.0"
description = "Engineering artifact store and retrieval"
authors = ["urbra <urbrain369@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
